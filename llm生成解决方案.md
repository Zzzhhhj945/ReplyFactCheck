这个任务的核心不是“简单判断像不像幻觉”，而是要把 **系统回复** 和 **知识库** 做逐条对齐，并识别出：编造、偏差、越界、遗漏、安全误导。推荐用 **规则预筛 + LLM 裁判 + 人工可解释输出** 的混合方案。下面是一个可落地的思路。

---

## 1. 先定义幻觉分类体系

可以直接沿用/细化 ground_truth 里的类型，分成 5 个大类：

| 大类 | 子类 | 判定标准 | 严重度 |
|---|---|---|---|
| 事实编造 | 参数编造、政策编造、优惠编造、信息编造 | 知识库没有，或与知识库明显矛盾 | 高/中 |
| 政策/信息偏差 | 政策偏差 | 部分正确、部分错误，数字/渠道/时效不一致 | 中 |
| 能力越界 | 能力越界 | 知识库明确“未接入接口/无功能”，回复却说“已查到/已修改/已升级/已发到账户” | 高 |
| 安全误导 | 安全误导 | 知识库有风险提示，回复反向承诺“放心用” | 高 |
| 信息遗漏 | 信息遗漏 | 知识库有关键限制/评价，回复遗漏导致建议不准 | 低/中 |
| 正确 | 无 | 回复与知识库一致 | 无 |

严重度建议：
- S3 高：安全误导、能力越界、严重参数/政策编造  
- S2 中：优惠编造、信息编造、政策偏差  
- S1 低：信息遗漏、轻微偏差  
- S0 无：正确回复

---

## 2. 检测工具实现思路

### 2.1 输入与输出

输入 `replies.json` 每条：
- `id`
- `user_question`
- `system_reply`
- `knowledge_base`

输出 `results.json`：
```json
{
  "id": "h01",
  "is_hallucination": true,
  "hallucination_type": "政策编造",
  "severity": "高",
  "evidence": "知识库为7天无理由，回复称30天且运费商家承担",
  "confidence": 0.98
}
```

### 2.2 规则预筛层

先做低成本、高确定性的规则检测：

1. **知识库为“无”或“未接入接口”**
   - 如果 `knowledge_base` 含“无（客服系统未接入……”且回复出现：
     - “已帮您查”“查到”“已修改”“已升级”“发到您账户”“直接抵扣”
   - 直接判 `能力越界`，高严重度。
   - 对应：h03、h10、h14、h18。

2. **数字/实体抽取比对**
   - 正则抽取：`30天`、`7天`、`5.3`、`5.0`、`48小时`、`24小时`、`40ms`、`80ms`、`满300减50`、`顺丰`、`中通/韵达/圆通`、`Type-C`、`USB-A`。
   - 如果回复数字/实体与知识库不一致，判 `参数编造`、`政策编造`、`优惠编造`、`政策偏差`。

3. **关键词表**
   - 高风险词：`NFC`、`头层牛皮`、`纸质发票`、`线下体验店`、`学生优惠`、`工单升级`、`孕妇放心使用`。
   - 如果知识库没有这些信息，或明确否定，则判编造/误导。

4. **安全词**
   - 知识库含 `孕妇`、`视黄醇`、`咨询医生`，回复含 `放心使用`，判 `安全误导`，高严重度。

### 2.3 LLM 裁判层

对规则无法覆盖的语义偏差、遗漏、部分正确，用 LLM 做判断。Prompt 可这样设计：

```text
你是客服回复幻觉检测器。请根据知识库判断系统回复是否幻觉。

分类：
- 参数编造：产品参数与知识库不符或知识库没有
- 政策编造：退货/发票/发货等政策被编造
- 政策偏差：部分正确部分错误
- 优惠编造：不存在的优惠券/活动
- 信息编造：地址、门店、品牌关系等被杜撰
- 能力越界：知识库说未接入接口/无功能，回复却说已查询/已修改/已升级
- 安全误导：忽略知识库风险提示，给出不安全承诺
- 信息遗漏：遗漏知识库关键限制，导致建议不准
- 无幻觉：与知识库一致

知识库：{knowledge_base}
用户问题：{user_question}
系统回复：{system_reply}

输出 JSON：
{"is_hallucination": true/false, "hallucination_type": "...", "severity": "高/中/低/无", "evidence": "...", "confidence": 0-1}
```

温度设为 0，要求只输出 JSON。few-shot 可以放 h01、h12、h20 三个例子，帮助模型理解“编造”“正确”“遗漏”的边界。

### 2.4 融合策略

- 规则高置信命中：直接输出。
- 规则未命中：交给 LLM。
- 规则和 LLM 冲突：以证据更具体的一方为准，并降低 confidence。
- `h20` 这类遗漏型，建议标为 `信息遗漏`，严重度低，并在 README 说明边界模糊。

---

## 3. 验证检出率

读取 `ground_truth.json`，按 `id` 对齐预测结果。正类 = `is_hallucination=true`。

20 条中人工标注：
- 正类 18 条：h01-h11、h13-h15、h17-h20
- 负类 2 条：h12、h16

计算：
```text
TP：预测为幻觉，真实也是幻觉
FP：预测为幻觉，真实不是
FN：预测不是幻觉，真实是
TN：预测不是幻觉，真实也不是

准确率 = (TP + TN) / 总数
精确率 = TP / (TP + FP)
召回率 = TP / (TP + FN)
F1 = 2 * 精确率 * 召回率 / (精确率 + 召回率)
```

如果工具输出与 ground_truth 完全一致：
- TP=18，FP=0，FN=0，TN=2
- 准确率=100%，精确率=100%，召回率=100%，F1=100%

如果工具“全部判为幻觉”：
- TP=18，FP=2，FN=0，TN=0
- 精确率=90%，召回率=100%，准确率=90%
- 误报 h12、h16

所以关键不是把所有都判成幻觉，而是 **不要把 h12、h16 误报**，同时尽量不漏 h20。

---

## 4. 误判原因分析

容易误判的 case：

1. **部分正确，部分错误**
   - h04：电子发票正确，但纸质发票和“备注里填”错误。
   - 工具如果只匹配到“电子发票”，容易判正确。

2. **偏差 vs 编造边界模糊**
   - h08：发货 24 小时变 48 小时，快递从中通/韵达/圆通变顺丰。
   - 看起来是“偏差”，但也是事实错误。

3. **信息遗漏没有直接矛盾**
   - h20：知识库有“30% 用户反馈偏大半码”，回复说“尺码标准”。
   - 没有明显反义词，LLM 容易漏检。

4. **安全误导需要语义理解**
   - h13：知识库说含视黄醇衍生物、孕妇咨询医生，回复说“放心使用”。
   - 需要识别“风险提示被忽略/反向承诺”。

5. **能力越界依赖“知识库为无”的元信息**
   - h03、h10、h14、h18：知识库明确未接入接口，但回复说已查/已改/已升级。
   - 如果模型忽略“无（未接入……）”，会误判为正常回复。

6. **正确样本容易被过度检测**
   - h12：货到付款、支付方式完全正确。
   - h16：色差说明与知识库一致。
   - 如果规则太激进，会误报。

---

## 5. 推荐项目结构与交付物

```text
task4-hallucination-detector/
├── data/
│   ├── replies.json
│   └── ground_truth.json
├── src/
│   ├── detector.py      # 主检测器
│   ├── rules.py         # 规则预筛
│   ├── llm_client.py    # 真实 API 或 mock
│   ├── evaluate.py      # 计算指标
│   └── schema.py        # 分类定义
├── app.py               # Streamlit 展示
├── README.md
└── requirements.txt
```

README 要写：
- 幻觉分类定义与严重度
- 检测方法：规则 + LLM + 融合
- 检出率：TP/FP/FN/TN、准确率、精确率、召回率、F1
- 漏检/误报清单
- 误判原因
- AI 工具使用情况：如 Cursor/ChatGPT/Claude 辅助生成 prompt、代码、测试
- mock 模式说明：无 API 时用关键词映射模拟 LLM 返回

截图建议：
1. IDE/Agent 工具截图：VSCode/Cursor 中项目结构、代码、Prompt。
2. 终端运行截图：`python evaluate.py` 输出指标。
3. 运行结果截图：Streamlit 页面展示 20 条检测结果、混淆矩阵、漏检/误报。

线上地址：
- 可用 Streamlit Cloud、HuggingFace Spaces 或 Vercel 部署。
- GitHub 仓库附 README 和截图即可。

---

## 6. 一句话总结

先定义“编造/偏差/越界/安全/遗漏”分类，再用规则抓数字、实体、接口越界，用 LLM 判语义偏差和遗漏，最后与 ground_truth 对齐算漏检误报。重点防 h12/h16 误报，重点查 h20 遗漏，并给每条结果附 evidence 和 confidence。